# AGENTS.md

## Rule relationship

- Follow the workspace root `../../AGENTS.md` first.
- These project rules may supplement or tighten the root rules, never weaken them.

## Project workflow

- Work only inside this project unless explicitly authorized otherwise.
- Read `README.md`, `pyproject.toml`, and relevant documentation before changes.
- Preserve this project's independent Git repository and worktree boundaries.
- Do not read `.env.local`; do not install dependencies without explicit approval.
- Run focused CLI and Python tests for changed behavior.

## Handoff

- Record durable decisions, progress, next action, blockers, and verification
  results in project documentation before changing Agents.
- Do not rely on chat history or Agent-specific memory as the only handoff record.
